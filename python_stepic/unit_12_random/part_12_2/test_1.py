from random import sample, choice

# def generate_ip():
#     list = list(range(10))
#     a, b, c, d = sample(range(10), 3), sample(list, 3), choice(list), sample(list, 3)

# print(generate_ip())

print(sample(range(10), 3))
